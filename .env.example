# Optional API key for production. Leave empty for local/demo use.
# API_KEY=your-secret-key

# Enable verbose OpenAPI metadata (keep false in production)
APP_DEBUG=true

# Comma-separated allowed origins for CORS
CORS_ORIGINS=*
